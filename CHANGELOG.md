# 更新日志

## [v2.6.0] - 2026-08-16

### 新增

- ICO（Windows 图标）格式全功能支持，纯 System.Drawing 实现、零外部依赖：
  - 作为输入：自动解析 ICO 目录并选取最大的一帧；PNG 压缩帧与经典 DIB 帧（含调色板、AND 透明掩码）都能解码；
  - 作为输出：生成 PNG 压缩的单图 ICO（Windows Vista+ 兼容），最大 256×256，超出时报错并提示常见尺寸；
  - 与其它格式一致：支持三种缩放方式、批量、`-Format` / `-KeepSize` 互转、通配符与简易菜单（`7) ICO`）、全屏界面的“输出格式”循环。
- 命令行、简易菜单、全屏界面三处入口的格式列表均加入 ICO。
- 全屏界面顶部内置四步新手教程（① 添加 → ② 设置 → ③ 格式 → ④ 开始），新手双击即可上手。

### 说明

- ICO 输出为单张目标分辨率图像；与其它格式一致，透明背景落在白色画布上。

## [v2.5.0] - 2026-08-14

### 新增

- 格式转换：支持 JPG / PNG / BMP / GIF / TIFF / WebP 任意互转。
- 命令行新增 `-Format`（目标格式）和 `-KeepSize`（原分辨率、只转格式不缩放）。
- 全屏界面新增“输出格式”选择项和 `K` 键“原分辨率”开关，简易菜单同步增加对应问答。
- “原分辨率”选项：命令行可用 `-Width 0 -Height 0`，全屏界面在宽/高输入 `0` 或按 `K`，界面会显示“原分辨率（不缩放）”。

### 调整

- 输出文件名规则：保持原分辨率转换时不再追加 `_宽x高` 后缀，输出为 `原名.新扩展名`。

## [v2.4.0] - 2026-08-14

### 新增

- 支持 WebP 格式：`.webp` 可作为输入参与批量处理和三种缩放方式，也可以作为输出格式（默认沿用原扩展名，或用 `-Out 名称.webp` 转换）。
- WebP 编解码使用随附的 Google libwebp 官方工具（`tools/dwebp.exe`、`tools/cwebp.exe`），BSD-3-Clause 许可，详见 `THIRD_PARTY_NOTICES.md`。
- `-Quality` 参数现在同时作用于 WebP 输出。

### 修复

- `-Out` 传入绝对路径时不再因路径拼接失败而报错。

### 说明

- 除 WebP 外的所有格式仍然零依赖；WebP 需要脚本目录 `tools` 中的两个工具，缺失时程序会给出明确提示。
- 与现有格式一致，WebP 的透明背景会落在白色画布上；动画 WebP 只处理第一帧。

## [v2.3.0] - 2026-08-14

开源首发版。

### 修复

- 缩放方式排序：把“居中留白”调整为第 1 项并标记为“推荐”，全屏界面与简易菜单保持一致（1 = 留白，2 = 裁剪，3 = 拉伸）。
- 按键提示与行为：开始处理由 `Enter` 改为 `S`；`Enter` 仅用于编辑 / 确认项目，在“开始处理”上按 `Enter` 只提示改用 `S`，不再误触发转换。
- 界面底部品牌文字由 `800600TOOLS` 改为项目名“图片分辨率修改工具”。

## [v2.2.0] - 2026-08-07

开源前的本地版本。

### 新增

- 全屏终端界面（TUI）、批量处理、三种缩放方式、进度条与结果面板。
- 命令行模式与完整参数（`-Width` / `-Height` / `-Mode` / `-OutDir` 等）。

### 修复

- 控制台“快速编辑模式”导致的点击窗口后按键卡死。
- 多行粘贴路径识别、添加文件后残留按键误触发快捷键。
- 启动失败时静默卡死、进度条一闪而过等问题。
